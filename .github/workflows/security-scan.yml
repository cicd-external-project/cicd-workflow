# ╔══════════════════════════════════════════════════════════════════╗
# ║  REUSABLE: Security Scanning                                   ║
# ║  Dependency audit + license compliance                        ║
# ╚══════════════════════════════════════════════════════════════════╝
name: "Reusable: Security Scan"

on:
  workflow_call:
    inputs:
      working-directory:
        required: false
        type: string
        default: "."
        description: "Project directory"
      system-name:
        required: true
        type: string
        description: "System name for artifacts/display"
      node-version:
        required: false
        type: number
        default: 24
        description: "Node.js version"
      fail-on-high:
        required: false
        type: boolean
        default: true
        description: "Fail pipeline on HIGH severity vulnerabilities"
      checkout-ref:
        required: false
        type: string
        default: ""
        description: "Git ref to check out (workflow_run chains pass head_sha)"
    outputs:
      audit-result:
        description: "npm audit result"
        value: ${{ jobs.dependency-audit.outputs.result }}


permissions:
  contents: read

jobs:
  # ── Dependency Vulnerability Audit ───────────────────────────────
  dependency-audit:
    name: "Dependency Audit"
    runs-on: ubuntu-latest
    timeout-minutes: 30
    outputs:
      result: ${{ steps.audit.outcome }}
    defaults:
      run:
        working-directory: ./${{ inputs.working-directory }}
    steps:
      - name: Checkout Code
        uses: actions/checkout@v5
        with:
          ref: ${{ inputs.checkout-ref }}

      - name: Setup Node.js ${{ inputs.node-version }}
        uses: actions/setup-node@v5
        with:
          node-version: ${{ inputs.node-version }}

      - name: Install Dependencies
        run: |
          if [ -f package-lock.json ]; then
            npm ci --ignore-scripts
          else
            echo "No package-lock.json — falling back to npm install"
            npm install --ignore-scripts
          fi

      - name: Run npm audit
        id: audit
        env:
          FAIL_ON_HIGH: ${{ inputs.fail-on-high }}
        run: |
          echo "🔍 Running dependency audit..."
          npm audit --json > audit-report.json 2>&1 || true
          npm audit --audit-level=high 2>&1 || AUDIT_EXIT=$?

          if [ "${AUDIT_EXIT:-0}" -ne 0 ] && [ "${FAIL_ON_HIGH}" = "true" ]; then
            echo "❌ HIGH/CRITICAL vulnerabilities found"
            npm audit --audit-level=high 2>&1
            exit 1
          fi
          echo "✅ No HIGH/CRITICAL vulnerabilities"

      - name: Summarise Audit Results
        if: always()
        run: |
          if [ ! -f "audit-report.json" ]; then
            echo "⚠️ audit-report.json not found — skipping summary."
            exit 0
          fi

          if ! jq -e '.vulnerabilities' audit-report.json >/dev/null 2>&1; then
            echo "⚠️ Audit report has no vulnerabilities key — may be an older npm format."
            cat audit-report.json | head -60
            exit 0
          fi

          TOTAL=$(jq '[.vulnerabilities | to_entries[].value.severity] | length' audit-report.json)
          CRITICAL=$(jq '[.vulnerabilities | to_entries[].value | select(.severity=="critical")] | length' audit-report.json)
          HIGH=$(jq '[.vulnerabilities | to_entries[].value | select(.severity=="high")] | length' audit-report.json)
          MODERATE=$(jq '[.vulnerabilities | to_entries[].value | select(.severity=="moderate")] | length' audit-report.json)
          LOW=$(jq '[.vulnerabilities | to_entries[].value | select(.severity=="low")] | length' audit-report.json)
          INFO=$(jq '[.vulnerabilities | to_entries[].value | select(.severity=="info")] | length' audit-report.json)

          echo "╔══════════════════════════════════════════════════════╗"
          echo "║          DEPENDENCY AUDIT SUMMARY                    ║"
          echo "╠══════════════════════════════════════════════════════╣"
          echo "║ Total vulnerabilities:  ${TOTAL}                     ║"
          echo "║   Critical:            ${CRITICAL}                   ║"
          echo "║   High:                ${HIGH}                       ║"
          echo "║   Moderate:            ${MODERATE}                   ║"
          echo "║   Low:                 ${LOW}                        ║"
          echo "║   Info:                ${INFO}                       ║"
          echo "╚══════════════════════════════════════════════════════╝"

          if [ "${CRITICAL}" -gt 0 ] || [ "${HIGH}" -gt 0 ]; then
            echo ""
            echo "🔴 Packages with HIGH/CRITICAL vulnerabilities:"
            jq -r '.vulnerabilities | to_entries[] | select(.value.severity == "critical" or .value.severity == "high") | "  • \(.key) (\(.value.severity)) — via \(.value.via | if type == "array" then map(if type == "string" then . else .title // .url // "direct" end) | join(", ") else . end)"' audit-report.json
          fi

      - name: Upload Audit Report
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: ${{ inputs.system-name }}-audit-report
          path: ${{ inputs.working-directory }}/audit-report.json
          retention-days: 30
          if-no-files-found: ignore

  # ── License Compliance ───────────────────────────────────────────
  license-check:
    name: "License Compliance"
    runs-on: ubuntu-latest
    timeout-minutes: 30
    defaults:
      run:
        working-directory: ./${{ inputs.working-directory }}
    steps:
      - name: Checkout Code
        uses: actions/checkout@v5
        with:
          ref: ${{ inputs.checkout-ref }}

      - name: Setup Node.js ${{ inputs.node-version }}
        uses: actions/setup-node@v5
        with:
          node-version: ${{ inputs.node-version }}

      - name: Install Dependencies
        run: |
          if [ -f package-lock.json ]; then
            npm ci --ignore-scripts
          else
            echo "No package-lock.json — falling back to npm install"
            npm install --ignore-scripts
          fi

      - name: Check Licenses
        run: |
          # Print the full license summary for visibility (never fails the build).
          npx license-checker --production --excludePrivatePackages --summary || true
          # Fail ONLY when a dependency actually uses a forbidden (copyleft /
          # non-commercial) license. Unlicensed / UNKNOWN packages — including
          # the project's own root package when it omits a "license" field — do
          # not fail the build, so generated customer pipelines pass cleanly.
          npx license-checker --production --excludePrivatePackages --failOn \
            "GPL-1.0;GPL-1.0-only;GPL-1.0-or-later;GPL-2.0;GPL-2.0-only;GPL-2.0-or-later;GPL-3.0;GPL-3.0-only;GPL-3.0-or-later;AGPL-1.0;AGPL-1.0-only;AGPL-1.0-or-later;AGPL-3.0;AGPL-3.0-only;AGPL-3.0-or-later;SSPL-1.0;BUSL-1.1;CC-BY-NC-4.0;CC-BY-NC-SA-4.0"
          echo "✅ No forbidden (copyleft/non-commercial) licenses found"
